<h3>Add viewing for <%= @room.name %></h3>

<%= render 'errors' %>
<%= render 'form' %>

<div class="calendar_container">
  <div id="calendar">
  </div>
  <FORM>
  <INPUT TYPE="button" onClick="history.go(0)" VALUE="Save Changes">
  </FORM>
  <br>
</div>
