<h3>Edit booking for <%= @room.name %></h3>
<%= render 'errors' %>
<%= render 'form' %>
<div class="calendar_container">
  <div id="calendar_bookings">
  </div>
	<form>
		<input type="button" onClick="history.go(0)" value="Save Changes">
	</form>
	<br>
</div>
