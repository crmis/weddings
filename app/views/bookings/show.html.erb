<div class="main-form">
  <fieldset>
    <h3>Booking Confirmation</h3>

    <p> <%= @booking.user.user_info %>, your booking is confirmed for the
      <%= @room.name %> on:
      <br>
      <br>
      <%= @booking.start_time.strftime('%e %b %Y %H:%M%p') %>
      until
      <%= (@booking.calculate_end_time + 60).strftime('%e %b %Y %H:%M%p') %>

    <p>
      <strong> Your added extras are: </strong>
      <% @booking.extras.each do |e| %>
          <%= e.name %> |
      <% end %>
    </p>
  </fieldset>
</div>
